# Backend

Prompt 1 will initialize this directory as a FastAPI + Python 3.12 + Pydantic + pytest service.

Planned baseline:

- `GET /health`
- Pydantic settings
- local CORS for the Vite frontend
- deterministic fixture mode with no external API keys
